#include<iostream>
using namespace std;
int main()
{
    int x,y,t;
    cout<<"side: ";
    cin>>x>>y>>t;
    if (x==y==t)
    {
        cout<<"Equilateral triangle";
    }
    else if (x==y!=t || x==t!=y || y==t!=x)
    {
        cout<<"Isosceles triangle";
    }
    else
    {
        cout<<"Different sided triangle";
    }
    
}
